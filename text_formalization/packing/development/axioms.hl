

(* let SET_OF_LEFT_ACTION_LIST_TRUNCATE_GEN = prove_by_refinement ( *)

g `!i j ul p.
     (?u0 u1 u2 u3. ul = [u0; u1; u2; u3:A]) /\
     i <= 3 /\ i <= j /\ p permutes 0..i - 1
     ==> set_of_list (truncate_simplex j (left_action_list p ul)) =
         set_of_list (truncate_simplex j ul)`;;
e (REPEAT STRIP_TAC);;
(* bat chuoc chung minh trong bo de 79 marchal_cells_2 *)
e (KY_CHEAT_TAC);;
let SET_OF_LEFT_ACTION_LIST_TRUNCATE_GEN = top_thm();;

