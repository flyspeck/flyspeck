

exception Unstable;;

type interval = {
  lo : float;
  hi : float;
};;

type line = {
  f : interval;
  df : (interval) list;
};;

type taylor_interval = {
  l : line;
  w : float list;
  dd : interval list list;
};;


