This directory contains the files that build the Flyspeck project.

ocamlinit_hol_light.ml:  Sample ML file for building the project. 
    It loads all other files as needed, including HOL Light.

strictbuild.hl: called by ocamlinit_hol_light.ml.

build.hl: List of project files used by strictbuild.hl.

checkpoint_flyspeck, restart_flyspeck, boot.ml, boot.hl: used for checkpointing the project.

computational_build.hl: This files organizes the informal calculations.  It is
			not needed in the formal proof.

