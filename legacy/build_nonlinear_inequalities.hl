



help "EQ_MP";;
help "PROVE_HYP";;

    let all_idv = Break_case_exec.all_idv;;

    let prep_idv = map (fun t -> t.idv) !Prep.prep_ineqs;;

    setify all_idv = setify prep_idv;;  (* true *)

  let _ = new_definition (mk_eq (`prepared_nonlinear:bool`,ineq_conj)) in
    ();;

   List.length prep_thms;;     

"nonlinear/mk_all_ineq.hl";
